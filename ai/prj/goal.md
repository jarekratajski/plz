# Goal

I want to create a cli tool `plz` that will do bash work defined in line for instance:

if You write:

plz stop all docker containers

it should automatically run bash commands to find and stop all running docker containers


# Implementation

Tool should connect to ai agent - choose one easy to use, user will have to login first (can be claude, can be chat gpt - choose one)

After each step revisit plan.md and correct if sth is wrong

When in doubt ask developer (me) for details

otherwise continue developing

write implementation details in details.md


# Requirements

Initially before executing command show user proposed execution like (ls -lrct) and ask for confirmation (y/n)


